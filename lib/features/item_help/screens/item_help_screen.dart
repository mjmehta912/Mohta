import 'package:flutter/material.dart';

class ItemHelpScreen extends StatelessWidget {
  const ItemHelpScreen({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
